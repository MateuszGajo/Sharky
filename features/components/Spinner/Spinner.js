import React from "react";
import ClipLoader from "react-spinners/ClipLoader";
const Spinner = () => {
  return <ClipLoader size={150} color={"#21bf73"} loading={true} />;
};

export default Spinner;
